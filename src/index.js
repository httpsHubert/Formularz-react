import React from 'react';
import ReactDOM from 'react-dom/client';
import Wypisywanie from './JSX/DodawanieOsoby'

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Wypisywanie/>
  </React.StrictMode>
);

